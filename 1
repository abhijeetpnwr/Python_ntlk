# Text processing with nltk python module

1.Install nltk module
2.While nltk.download() if you are using headless way (shell) , after listing pacakges with nltk downlaod menu enter all. So it will install the modules.
